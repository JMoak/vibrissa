{
  "name": "vibrissa",
  "version": "0.1.0",
  "description": "Ultra-light JSON-driven MCP integration test runner",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "vib-test": "bin/vib-test.js"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs",
      "types": "./dist/index.d.ts"
    },
    "./cli": {
      "import": "./dist/cli.js",
      "require": "./dist/cli.cjs"
    }
  },
  "files": ["dist", "bin", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsup src/cli.ts src/index.ts --format esm,cjs --dts --clean --sourcemap",
    "dev": "tsup src/cli.ts src/index.ts --format esm,cjs --dts --watch --sourcemap",
    "typecheck": "tsc --noEmit",
    "test": "jest --config ./jest.config.cjs",
    "test:ci": "jest --config ./jest.config.cjs --reporters=default --reporters=jest-junit",
    "lint": "biome check . --write",
    "lint:fix": "biome check . --write --unsafe"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JMoak/vibrissa.git"
  },
  "keywords": ["mcp", "integration", "testing"],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/JMoak/vibrissa/issues"
  },
  "homepage": "https://github.com/JMoak/vibrissa#readme",
  "devDependencies": {
    "@swc/jest": "^0.2.39",
    "@types/node": "^20.19.13",
    "@biomejs/biome": "^1.8.3",
    "jest": "^30.1.3",
    "jest-junit": "^16.0.0",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  }
}
