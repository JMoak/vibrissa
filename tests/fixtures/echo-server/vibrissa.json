{
  "$schema": "../../../schema/vibrissa.schema.json",
  "server": { "cmd": "node", "args": ["server/index.js"], "cwd": "." },
  "globs": ["cases/**/*.json", "!cases/**/*.fail.json"],
  "timeoutMs": 3000,
  "failFast": false
}
